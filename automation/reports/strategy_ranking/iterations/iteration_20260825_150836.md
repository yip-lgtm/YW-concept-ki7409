# Strategy Iteration - 2026-08-25

## Worst Performer
**RSI Divergence** (rank #9)

- Profit Factor: 0.92
- Total R: -8
- Win Rate: 45.0%
- Trades: 120
- 7d Avg PF: 0.92

## Current Parameters
```json
{
  "weight": 1.1,
  "timeframe": "1/3/5min",
  "indicators": [
    "rsi",
    "divergence"
  ]
}
```

## LLM-Suggested Parameters
```json
{
  "weight": 0.65,
  "timeframe": "5min",
  "indicators": [
    "rsi",
    "divergence",
    "volume",
    "ema"
  ],
  "r_multiples": [
    0.8,
    1.2,
    1.8,
    2.5,
    3.5
  ],
  "rationale": "Strategy is unprofitable (PF 0.92, R -8 over 120 trades) with sub-50% win rate, indicating noisy/false divergence signals. Reducing weight from 1.1 to 0.65 deprioritizes this underperformer in the daily ranking. Switching to a single 5min timeframe eliminates lower-timeframe noise that likely generates false divergence triggers (45% WR suggests many failed setups). Adding volume confirms that divergences are backed by genuine exhaustion (declining volume on new extremes strengthens signal validity), while an EMA filter (e.g., 50/200 EMA) ensures trades align with the prevailing trend rather than counter-trend mean-reversion traps. Tightening r_multiples\u2014especially T1 at 0.8R\u2014addresses the poor win rate by banking quick profits before momentum reversals erase gains, while T2-T5 at 1.2/1.8/2.5/3.5 preserve upside when the divergence resolves into a full trend. Combined effect should filter low-quality setups and improve realized R-multiple distribution."
}
```

## LLM Response (raw)
<think>The strategy is underperforming with a profit factor below 1.0 (0.92), negative total R (-8), and a win rate of only 45%. Let me analyze this and suggest improvements.

Key issues:
1. Profit factor < 1 means losses exceed profits
2. Negative R (-8) over 120 trades confirms unprofitable
3. 45% win rate is below the typical 50%+ needed for profitability with standard R:R
4. Only 7 days of history, so the strategy is consistently poor

Analysis for improvements:

1. **Weight**: Currently 1.1 but strategy is losing. Should reduce weight since it's underperforming. Suggest around 0.6-0.7.

2. **Timeframe**: The strategy uses 1/3/5min multi-timeframe. RSI divergence works better when noise is reduced. I should suggest a single, cleaner timeframe. 5min alone might filter noise better, or p

## Action
Review suggested params. If confident, manually update STRATEGY_PARAMS in:
- `automation/src/yw_indicators.py` (for YW strategies)
- `automation/src/ocs_btc_5m.py` (for OCS)

Then re-run `python scripts/strategy_ranking.py` to verify improvement.
